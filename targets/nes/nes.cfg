# NROM-256: 32 KB PRG, 8 KB CHR, no mapper, no bank switching.
# The smallest thing every NES emulator agrees about.

MEMORY {
    ZP:      start = $0002, size = $00FE, type = rw, define = yes;
    OAM:     start = $0200, size = $0100, type = rw, define = yes;
    RAM:     start = $0300, size = $0500, type = rw, define = yes;

    HEADER:  start = $0000, size = $0010, type = ro, file = %O, fill = yes;
    PRG:     start = $8000, size = $7FFA, type = ro, file = %O, fill = yes, fillval = $FF;
    VECTORS: start = $FFFA, size = $0006, type = ro, file = %O, fill = yes;
    CHR:     start = $0000, size = $2000, type = ro, file = %O, fill = yes;
}

SEGMENTS {
    ZEROPAGE: load = ZP,      type = zp;
    OAMBUF:   load = OAM,     type = bss, define = yes;
    BSS:      load = RAM,     type = bss, define = yes;

    HEADER:   load = HEADER,  type = ro;
    CODE:     load = PRG,     type = ro, start = $8000;
    RODATA:   load = PRG,     type = ro;
    VECTORS:  load = VECTORS, type = ro;
    CHARS:    load = CHR,     type = ro;
}
